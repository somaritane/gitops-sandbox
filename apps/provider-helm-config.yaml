apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: provider-helm-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: default
  source:
    path: provider-helm-config/
    repoURL: https://github.com/somaritane/gitops-sandbox.git
    targetRevision: deploy-providers-with-crossplane
  destination:
    namespace: crossplane-system
    server: 'https://kubernetes.default.svc'
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
